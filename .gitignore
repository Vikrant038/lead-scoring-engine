# Dependencies
node_modules/

# Build output
dist/

# Secrets (SEC-02)
.env
.env.local

# Runtime data (keep sample data tracked explicitly)
logs/*
input/*
output/*
data/sessions/*
# SQLite database (Phase 1) — runtime artefact, not committed
data/*.db
data/*.db-shm
data/*.db-wal
# E2E-created personas (the packaged default-icp.json stays tracked)
personas/e2e-*.json
personas/.trash/
!**/.gitkeep
!**/*.sample.json

# OS / editor
.DS_Store
*.log
coverage/
.playwright/
test-results/
playwright-report/
demo-output/
.vercel/
.omo/
